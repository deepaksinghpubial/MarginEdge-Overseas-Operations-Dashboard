# Generated offline copies (tools/make-standalone.py). ~1.9 MB each and rebuilt
# on demand, so they are deliberately not committed - keep them on your own
# machine or in Drive, not in git history.
*-standalone.html
backups/
